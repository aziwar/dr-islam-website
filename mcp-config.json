{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["@context7/mcp-server"],
      "env": {},
      "description": "Official library documentation lookup - Get up-to-date docs for any library"
    },
    "sequential": {
      "command": "npx", 
      "args": ["@sequential/mcp-server"],
      "env": {},
      "description": "Complex multi-step analysis - Advanced reasoning and problem solving"
    },
    "magic": {
      "command": "npx",
      "args": ["@magic/mcp-server"], 
      "env": {},
      "description": "Modern UI component generation - Create beautiful React/Vue components"
    },
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp-server"],
      "env": {},
      "description": "Browser automation and testing - Advanced E2E testing capabilities"
    }
  },
  "tools": {
    "allowedDomains": ["dr-elsagher.com", "localhost"],
    "browserSettings": {
      "headless": false,
      "viewport": {
        "width": 1920,
        "height": 1080
      }
    }
  },
  "workflows": {
    "webTesting": {
      "servers": ["playwright", "context7"],
      "description": "Comprehensive web testing with documentation lookup"
    },
    "uiDevelopment": {
      "servers": ["magic", "context7", "sequential"],
      "description": "UI component development with analysis"
    },
    "debugging": {
      "servers": ["sequential", "playwright", "context7"],
      "description": "Advanced debugging and analysis"
    }
  }
}