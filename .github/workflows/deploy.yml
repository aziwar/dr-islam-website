# Auto-deploy to Cloudflare Workers on push to master
name: Deploy to Cloudflare Workers

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build project (remove console.logs)
        run: npm run build
      
      - name: Verify Wrangler installation and credentials
        run: |
          echo "🔧 Checking Wrangler version..."
          npx wrangler --version
          
          echo "🔍 Testing Cloudflare authentication..."
          if ! npx wrangler whoami 2>&1; then
            echo "❌ Authentication failed - diagnosing issues..."
            
            # Check if secrets are available
            if [ -z "$CLOUDFLARE_API_TOKEN" ]; then
              echo "💥 CRITICAL: CLOUDFLARE_API_TOKEN secret is missing or empty"
              echo "📋 Please add CLOUDFLARE_API_TOKEN to GitHub repository secrets"
              echo "🔗 Go to: Settings → Secrets and variables → Actions → New repository secret"
              exit 1
            else
              echo "✅ CLOUDFLARE_API_TOKEN is present (${#CLOUDFLARE_API_TOKEN} characters)"
            fi
            
            if [ -z "$CLOUDFLARE_ACCOUNT_ID" ]; then
              echo "💥 CRITICAL: CLOUDFLARE_ACCOUNT_ID secret is missing or empty"
              echo "📋 Please add CLOUDFLARE_ACCOUNT_ID to GitHub repository secrets"
              echo "🔗 Go to: Settings → Secrets and variables → Actions → New repository secret"
              exit 1
            else
              echo "✅ CLOUDFLARE_ACCOUNT_ID is present (${#CLOUDFLARE_ACCOUNT_ID} characters)"
            fi
            
            # Test API token validity
            echo "🧪 Testing API token validity..."
            HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
              -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" \
              "https://api.cloudflare.com/client/v4/user/tokens/verify")
            
            if [ "$HTTP_STATUS" = "200" ]; then
              echo "✅ API token is valid"
            else
              echo "❌ API token verification failed (HTTP $HTTP_STATUS)"
              case $HTTP_STATUS in
                "401") echo "🚫 Token is invalid or expired - please regenerate" ;;
                "403") echo "🚫 Token has insufficient permissions - needs Workers:Edit + Account:Read" ;;
                *) echo "🚫 Unexpected error - check token and network connectivity" ;;
              esac
              exit 1
            fi
            
            echo "🤔 Authentication setup appears correct but wrangler whoami failed"
            echo "🔄 This might be a temporary issue - deployment will retry automatically"
          else
            echo "✅ Authentication successful!"
          fi
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      
      - name: Deploy to Cloudflare Workers (with retry)
        run: |
          echo "🚀 Starting main worker deployment..."
          for i in {1..3}; do
            echo "📡 Deployment attempt $i of 3 for main worker..."
            
            if npx wrangler deploy --verbose 2>&1; then
              echo "✅ Main worker deployment successful!"
              echo "🌐 Worker deployed to: https://dr-islam-website.YOUR_SUBDOMAIN.workers.dev"
              break
            else
              DEPLOY_EXIT_CODE=$?
              echo "❌ Main worker deployment failed (exit code: $DEPLOY_EXIT_CODE)"
              
              if [ $i -eq 3 ]; then
                echo "💥 All main worker deployment attempts failed"
                echo "🔍 Common causes:"
                echo "   • Invalid/expired CLOUDFLARE_API_TOKEN"
                echo "   • Missing Workers permissions on API token"
                echo "   • Invalid CLOUDFLARE_ACCOUNT_ID"
                echo "   • Worker name already exists in different account"
                echo "   • Cloudflare API outage"
                exit 1
              fi
              
              echo "🔄 Retrying main worker deployment in 30 seconds..."
              sleep 30
            fi
          done
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      
      - name: Deploy Contact Form Worker (with retry)
        run: |
          echo "📧 Starting contact form worker deployment..."
          for i in {1..3}; do
            echo "📡 Deployment attempt $i of 3 for contact form worker..."
            
            if npx wrangler deploy -c wrangler-contact.toml --verbose 2>&1; then
              echo "✅ Contact form worker deployment successful!"
              echo "📬 Contact form available at: https://dr-elsagher.com/api/contact"
              break
            else
              DEPLOY_EXIT_CODE=$?
              echo "❌ Contact form worker deployment failed (exit code: $DEPLOY_EXIT_CODE)"
              
              if [ $i -eq 3 ]; then
                echo "💥 All contact form worker deployment attempts failed"
                echo "🔍 This might be due to:"
                echo "   • Domain routing configuration issues"
                echo "   • Missing zone permissions on API token"  
                echo "   • DNS zone not properly configured"
                echo "   • Contact form worker name conflicts"
                exit 1
              fi
              
              echo "🔄 Retrying contact form worker deployment in 30 seconds..."
              sleep 30
            fi
          done
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
